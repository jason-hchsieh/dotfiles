Host github.com
    Hostname ssh.github.com
    Port 443
    User git
{{- if env "https_proxy" }}
    ProxyCommand nc -X connect -x {{ trimPrefix "http://" (env "https_proxy") }} %h %p
{{- end }}

{{- /* vim: set filetype=sshconfig: */}}
